# Project Context: audio2midi

All project guidance lives in [CLAUDE.md](CLAUDE.md) — environment rules, commands, architecture,
and conventions. Read that file first; it is the single source of truth and is kept current.

This file exists only as a pointer, so the two do not drift apart.
